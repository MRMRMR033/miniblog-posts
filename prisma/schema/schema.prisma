generator client {
    provider = "prisma-client-js"
    previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
    provider = "mysql"
    url = "mysql://root:Tigre033@localhost:3306/miniblogdb"
}